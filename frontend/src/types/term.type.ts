export type TermDataItem = {
    id: string,
    code: string,
    name: string,
    type: string,
    credits: number,
    day: number,
    scheduleId: string,
    lesson: string,
    isRegistered: boolean,
    isApproved: boolean
}